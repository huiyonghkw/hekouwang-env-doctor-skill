#!/usr/bin/env bash
# README 演示用 · 回放脚本
#
# 数据来源：作者本机 2026-07-18 真实体检结果（scan.sh 实测输出，非虚构）。
# 为控制 GIF 时长，此处回放已采集的结论，不重跑 20 秒的实际扫描。
# 想看真扫描：bash scripts/scan.sh
#
# ⛔ 本脚本同样只负责打印，不执行任何删除。

B=$'\033[1m'; D=$'\033[2m'; R=$'\033[0m'
O=$'\033[38;5;208m'; Y=$'\033[38;5;179m'; RD=$'\033[38;5;167m'
G=$'\033[38;5;71m';  P=$'\033[38;5;140m'; C=$'\033[38;5;73m'

p() { printf '%b\n' "$1"; sleep "${2:-0.28}"; }

p ""
p "${D}› 磁盘满了，帮我看看什么占了空间${R}" 0.9
p ""
p "${C}⏳ 扫描中（只读，不改动任何文件）…${R}" 1.3
p ""
p "${B}体检结论${R}" 0.5
p "  460 GB 的盘用了 ${B}338 GB（79%）${R}"
p "  扫到约 50 GB 缓存与残留，其中 ${O}${B}9.9 GB 属于「你已经不用的工具」${R}"
p "  最该看的一条：${B}~/.nvm 占 9.8 GB${R}，但你的 node 实际来自 fnm" 1.1
p ""
p "${O}${B}🟠 残留${R}${O} — 可放心清 · 合计 9.9 GB${R}" 0.5
p "   ${B}~/.nvm${R}      ${O}9.8 GB${R}   ${D}未加载 · 不在PATH · node由fnm提供 · 最后改动 2025-03${R}"
p "   ${B}~/.pyenv${R}    ${O}121 MB${R}   ${D}未加载 · 不在PATH · python由brew提供 · 最后改动 2024-07${R}" 0.9
p ""
p "${Y}${B}🟡 缓存${R}${Y} — 在用，删了会重下${R}" 0.4
p "   ~/.cache              17.8 GB"
p "   ~/.npm                 4.7 GB   ${D}npm cache clean --force${R}"
p "   ~/Library/Caches       7.1 GB" 0.8
p ""
p "${RD}${B}🔴 数据${R}${RD} — 删了找不回来，别碰${R}" 0.4
p "   ~/.cache/huggingface   7.3 GB   ${D}模型权重，重下要几小时${R}"
p "   Docker 容器            6.3 GB   ${D}卷里可能有数据库数据${R}" 0.9
p ""
p "${P}❓ 不确定${R} — 需要你判断" 0.4
p "   ~/.pyenv 的 versions/ 下还有一个 pypy3.10${D}，如果是你特意留的就别删${R}" 1.0
p ""
p "${G}${B}⛔ 我不代删${R}${G} —— 命令列给你，你自己跑。要我列出来吗？${R}" 1.6
p ""
